<section
    class="md:mx-auto text-center md:px-16 xl:px-18 bg-white xl:max-w-[1536px]">
    
    {# Conteneur principal de la section #}
    <div class="flex w-full flex-col items-center w-full gap-12">

        {# Titre de la section #}
        <h2 class="text-2xl md:text-4xl xl:text-6xl font-bold">
            Pourquoi votre
            <span class="text-sky-600">équilibre alimentaire</span>
            est essentiel
        </h2>

        {# Container des statistiques #}
        <div class="flex flex-col w-full md:flex-row justify-center items-start gap-12">

            {# Bloc 1 : Pourcentage d'adultes ne respectant pas les recommandations #}
            <div class="flex flex-col w-full items-center md:items-start gap-4">
                <div class="flex items-start gap-2">
                    <span class="text-6xl md:text-6xl font-bold">+68</span>  {# Valeur statistique #}
                    <span class="text-6xl font-semibold text-sky-600">%</span>  {# Symbole pourcentage #}
                </div>
                <p class="text-gray-600 max-w-xs font-semibold">
                    des adultes ne respectent pas les
                    <span class="text-sky-600">recommandations nutritionnelles</span>.
                </p>
            </div>

            {# Bloc 2 : Pourcentage de maladies chroniques liées au mode de vie #}
            <div class="flex flex-col w-full items-center md:items-start gap-4">
                <div class="flex items-end gap-2">
                    <span class="text-6xl md:text-6xl font-bold">80</span>  {# Valeur statistique #}
                    <span class="text-6xl font-semibold text-sky-600">%</span>  {# Symbole pourcentage #}
                </div>
                <p class="text-gray-600 max-w-xs font-semibold">
                    des
                    <span class="text-sky-600">maladies chroniques</span>
                    sont liées au mode de vie et à l’alimentation.
                </p>
            </div>

            {# Bloc 3 : Nombre de jours pour ressentir les effets d’un rééquilibrage #}
            <div class="flex flex-col w-full items-center md:items-start gap-4">
                <div class="flex items-end gap-2">
                    <span class="text-6xl md:text-6xl font-bold">15</span>  {# Nombre de jours #}
                    <span class="text-6xl font-semibold text-sky-600">j</span>  {# Symbole “jours” #}
                </div>
                <p class="text-gray-600 max-w-xs font-semibold">
                    suffisent pour ressentir les
                    <span class="text-sky-600">premiers effets</span>
                    d’un rééquilibrage.
                </p>
            </div>

            {# Bloc 4 : Premier levier d’énergie et de bien-être #}
            <div class="flex flex-col w-full items-center md:items-start gap-4">
                <div class="flex items-end gap-2">
                    <span class="text-6xl md:text-6xl font-bold">1</span>  {# Ordinal #}
                    <span class="text-6xl font-semibold text-sky-600">er</span>  {# Symbole “premier” #}
                </div>
                <p class="text-gray-600 max-w-xs font-semibold">
                    levier d’énergie, de concentration et de
                    <span class="text-sky-600">bien-être durable</span>.
                </p>
            </div>

        </div>  {# Fin du container des statistiques #}

    </div>  {# Fin du conteneur principal #}

</section>