<div {{attributes}}>

    <div class="flex mt-4">

        <div class="relative search_food_list w-1/4">
            {# <div class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none">
                <svg aria-hidden="true" class="w-5 h-5 text-gray-500 dark:text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path></svg>
            </div> #}
            <input
                type="search"
                data-model="query"
                class="block p-6 w-full rounded-full border border-slate-800"
                placeholder="Aliment ou plat"
                {# data-action="live#update" #}
            >
        </div>

        {# <input type="number" data-model="max">

        Generating a number between 0 and {{ max }}
        <strong>{{ this.randomNumber }}</strong>

        <button
            data-action="live#action"
            data-action-name="foo"
        >Reset Min/Max</button> #}

        <div class="flex ml-auto gap-4 items-center">
            {% for type in constant('App\\Service\\TypeDishHandler::TYPE_NAMES') %}
                <div class="px-4 text-white shadow-lg font-bold bg-cyan-800 rounded" style="background-color: rgb(129 140 248)">
                    <button 
                        data-action="live#action"
                        data-action-name="selectType(type={{ type }}"
                    >
                        {{ type|trans({}, 'dishes')|capitalize }}
                    </button>
                </div>
            {% endfor %}
        </div>

        {{ include('partials/_view_mode_list.html.twig') }}

        <div class="flex items-center gap-6 ml-6">

            {# {% set count_food = 26 %}
            <span class="text-right">
                {% trans with {'count_food' : count_food, 'foodgroup' : foodGroup.name} from 'app' into 'fr' %}food.list.count{% endtrans %}
            </span> #}
            {# {{ food.list.count|trans({'count_food' : count_food}, 'app') }} #}

            <select>
                <option><span class="font-bold">TRIER</span>: Pertinence</option>
                <option>Ordre alphabétique</option>
            </select>

        </div>

    </div>

    {# <div id="wrapper" class="wrapper view-mode-list grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 xl:grid-cols-4 2xl:grid-cols-5 mt-3 gap-2" data-loading="addClass(opacity-50)">      #}
    <div id="wrapper" class="wrapper mt-3 gap-2" data-loading="addClass(opacity-50)">     
    {# <div id="card-wrapper" class="list mt-3 gap-2" data-loading="addClass(opacity-50)">      #}

        {% if this.dishs|length > 0 %}   
            {% for dish in this.dishs %}
                {{ include("dish/_dish.html.twig") }}
            {% endfor %}
        {% else %}
            <div>No dishes found"</div>
        {% endif %}

    </div>

    <script> 
        const wrapper = document.getElementById("wrapper");
        const btnModeList = document.getElementById("view-mode-list");
        const btnModeGrid = document.getElementById("view-mode-grid");

        btnModeList.addEventListener("click", function (event) {
            // List view
            event.preventDefault();
            wrapper.classList.add("list");
            btnModeGrid.classList.remove("view-mode-switch__display--selected")
            this.classList.add("view-mode-switch__display--selected");
        });

        btnModeGrid.addEventListener("click", function (event) {
            // Grid view
            event.preventDefault();
            wrapper.classList.remove("list");
            btnModeList.classList.remove("view-mode-switch__display--selected");
            this.classList.add("view-mode-switch__display--selected");
        });
    </script>

</div>