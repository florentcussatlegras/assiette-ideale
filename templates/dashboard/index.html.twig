{% extends "base.html.twig" %}

{% block title %}{{ parent() }} - Tableau de bord{% endblock %}

{% block body %}

	{# <main role="main">
				
				    <div class="p-5">
				
				        {{ include("partials/_alert.html.twig") }}
				
				        <div class="flex flex-col w-4/5 mx-auto">
				            
				            <div class="flex flex-col gap-10 mb-5">
				
				                <div class="flex flex-row mt-4 items-center">
				                    <div class="flex flex-col gap-4 items-start">
				                        <h1 class="text-2xl">Bienvenue <span class="text-dark-blue font-bold tracking-tight">{{ app.user.username|capitalize }}</span></h1>
				                        <a class="border border-black font-bold px-4 py-2 rounded-full flex items-center gap-2 mr-4" href="{{ path('app_profile_index') }}"> 
				                            <svg fill="none" viewBox="0 0 24 24" stroke="currentColor" id="user" class="w-6 h-6 text-cool-gray-800 dark:text-cool-gray-200 group-hover:text-purple-600 group-focus:text-purple-600 dark:group-hover:text-purple-50 dark:group-focus:text-purple-50"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path></svg>
				                            <span>Voir mon profil</span> 
				                        </a>
				                    </div>
				                    <a type="button" class="ml-auto bg-gray rounded-full font-bold text-lg px-4 py-3 flex items-center gap-2" href="{{ path('app_dish_food_session_clear') }}" style="box-shadow: rgba(0, 0, 0, 0.12) 0px 0px 6px 0px, rgba(0, 0, 0, 0.12) 0px 6px 6px 0px"> 
				                        <svg viewBox="0 0 24 24" class="w-5 h-5" data-v-563d9226=""><path d="M17,13H13V17H11V13H7V11H11V7H13V11H17M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2Z" data-v-563d9226=""></path></svg>
				                        <span>Proposer une recette</span>
				                    </a>
				                </div>
				
				                <div class="flex items-center button-group mb-2">
				                
				                   
				
				                    {{ render(path('app_balance_sheet_show_imc_alert')) }}
				
				                     <div class="flex flex-col gap-4 items-start ml-auto"> 
				                        <h2 class="text-dark-blue font-bold ml-auto">Retrouver vos plats par semaine</h2>
				                        {{ render(controller('App\\Controller\\meal\\SearchController::searchWeekMenu')) }}
				                    </div>
				
				                </div>
				
				            </div>
				
				            <div class="grid grid-cols-12 gap-6">
				                {% for category in secondary %}
				                    {{ include('dashboard/_category.html.twig') }}
				                {% endfor %}
				            </div>
				            
				        </div>
				
				    </div>
				
				</main> #}

	<main role="main">

		<div class="flex flex-col w-full md:w-4/5 mx-auto py-12">

			<div class="flex flex-col md:flex-row md:items-center gap-2 justify-between w-full mb-4">

				<h1 class="text-4xl mb-0 font-bold relative">
					{% trans with {'username': "<span class=\"font-bold tracking-tight\">#{app.user.username}</span>" } from 'dashboard'%}dashboard.welcome{% endtrans %}
					<div class="absolute top-10 left-0 text-sm font-light text-neutral-500">Inscrit le {{ app.user.registerAt|date('d/m/Y') }}</div>
				</h1>

				<span class="my-4 md:my-0">
					{{ render(path('app_balance_sheet_show_imc_alert')) }}
				</span>
				
			</div>

			<div class="flex flex-col md:flex-row items-start md:items-end mb-6">

				<div class="flex flex-row gap-2 lg:gap-4 items-center">
					<a class="link-white rounded-lg font-bold py-2 flex items-center text-white bg-sky-600 shadow-soft px-2 text-base transition duration-300 hover:bg-sky-700" href="{{ path('app_profile_index') }}">
						<span>{% trans from 'dashboard' %}dashboard.buttons.see_profile{% endtrans %}</span>
					</a>
					<a href="{{ path('app_favorite_dishes_list') }}" class="font-bold text-base rounded-lg px-4 py-2 text-sky-600 hover:text-sky-700 bg-gray-200 hover:text-sky-500 current-shadow hover:shadow-lg">Mes plats préférés</a>
				</div>

				<div class="flex flex-col items-start md:items-end ml-0 md:ml-auto pt-6 md:pt-1 text-sm lg:text-base gap-1 mb-2 md:mb-0">
					<span class="font-bold text-right md:ml-auto">{% trans from 'dashboard' %}dashboard.search_menu_week{% endtrans %}</span>
					<div class="md:ml-auto">{{ render(controller('App\\Controller\\meal\\SearchController::searchWeekMenu')) }}</div>
				</div>

			</div>

			<div class="grid grid-cols-12 gap-6">
				{% for category in secondary %}
					{{ include('dashboard/_category.html.twig') }}
				{% endfor %}
			</div>

		</div>

	</main>

{% endblock %}
