{# Card de chargement avec animation pulse (placeholder) #}
<div class="content bg-white pt-2 pb-2 px-6 rounded-xl shadow-balanced flex-1 animate-pulse">

    {# Ligne principale simulant le titre / bilan du jour #}
    <div class="h-8 bg-gray-300 rounded my-4 mt-0 w-3/4">
        {# Texte invisible pour accessibilité / SEO #}
        <span class="opacity-0">Mes bilans pour la journée du lundi 8 février 2026</span>
    </div>

    {# Ligne simulant sous-titre / moyenne journalière #}
    <div class="h-6 bg-gray-200 rounded w-1/3">
        <span class="opacity-0">(moyenne journalière pour cette période)</span>
    </div>

    {# Ligne simulant message d’absence de repas #}
    <div class="h-4 bg-gray-200 rounded my-12 w-1/3">
        <span class="opacity-0">Aucun repas repertoriés pour cette période</span>
    </div>

</div>