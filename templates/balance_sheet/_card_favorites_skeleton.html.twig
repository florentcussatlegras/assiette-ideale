{# Placeholder animé pour plusieurs sections de contenu (squelette de chargement) #}
<div class="flex flex-col gap-4 items-start font-medium bg-white p-4 rounded-xl shadow-balanced animate-pulse w-full mt-2">

    {# Section 1 : titre et sous-texte simulés #}
    <div class="h-4 bg-gray-300 rounded w-1/3"></div>  {# Ligne principale (titre) #}
    <div class="h-3 bg-gray-200 rounded w-1/2"></div>  {# Ligne secondaire (description) #}

    {# Section 2 : titre et sous-texte simulés #}
    <div class="h-4 bg-gray-300 rounded w-1/3 mt-2"></div>  {# Ligne principale (titre) #}
    <div class="h-3 bg-gray-200 rounded w-1/2"></div>  {# Ligne secondaire (description) #}

    {# Section 3 : titre et sous-texte simulés #}
    <div class="h-4 bg-gray-300 rounded w-1/2 mt-2"></div>  {# Ligne principale (titre) #}
    <div class="h-3 bg-gray-200 rounded w-2/3"></div>  {# Ligne secondaire (description) #}

</div>